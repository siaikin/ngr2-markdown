import { TreeNodeToggleDirective } from './tree-node-toggle.directive';

describe('TreeNodeToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeNodeToggleDirective();
    expect(directive).toBeTruthy();
  });
});
