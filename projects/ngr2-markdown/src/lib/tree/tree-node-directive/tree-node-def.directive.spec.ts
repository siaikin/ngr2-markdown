import { TreeNodeDefDirective } from './tree-node-def.directive';

describe('TreeNodeDefDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeNodeDefDirective();
    expect(directive).toBeTruthy();
  });
});
