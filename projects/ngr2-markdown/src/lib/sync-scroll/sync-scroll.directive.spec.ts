import { SyncScrollDirective } from './sync-scroll.directive';

describe('SyncScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SyncScrollDirective();
    expect(directive).toBeTruthy();
  });
});
